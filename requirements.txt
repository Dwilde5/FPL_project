# FPL AI Agent — Python Dependencies
# Install with: pip install -r requirements.txt
#
# Phase gating: core dependencies are always installed.
# RL dependencies are commented out until Phase 5.

# ── Core ────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0            # Environment variable management

# ── Data manipulation ────────────────────────────────────────────────────────
pandas>=2.2.0
polars>=0.20.0                  # Fast columnar processing for Parquet files
numpy>=1.26.0
pyarrow>=15.0.0                 # Parquet read/write backend

# ── API access ───────────────────────────────────────────────────────────────
requests>=2.31.0

# ── Notebooks & visualisation ────────────────────────────────────────────────
jupyter>=1.0.0
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0

# ── RL (Phase 5+) — uncomment when starting the RL layer ────────────────────
# gymnasium>=0.29.0
# stable-baselines3>=2.3.0
# torch>=2.2.0                  # SB3 dependency — install CPU or CUDA build as appropriate
